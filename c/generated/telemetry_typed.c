/* Generated by tools/face_tss_codegen.py - DO NOT EDIT. */
/* Data type: FaceTSS.Telemetry. Codec over the flatcc runtime builder API,
 * mirroring c/src/envelope.c (no generated flatcc code needed). */

#include "telemetry_typed.h"

#include <stdlib.h>
#include <string.h>

#include <flatcc/flatcc_builder.h>
#include <flatcc/flatcc_endian.h>

/* Little-endian field readers (same approach as c/src/envelope.c). */
static uint16_t rd_u16(const uint8_t *p)
{ uint16_t v; memcpy(&v, p, 2); return v; }
static uint32_t rd_u32(const uint8_t *p)
{ uint32_t v; memcpy(&v, p, 4); return v; }

/* Parse a table header: vtable location and vtable/table sizes. */
static int table_header(const uint8_t *buf, size_t len, uint32_t table,
                        uint32_t *vtable_out, uint32_t *vsize_out,
                        uint32_t *tsize_out)
{
    int32_t soff = (int32_t)rd_u32(buf + table);
    uint32_t vtable = table - (uint32_t)soff;
    uint32_t vsize, tsize;
    if (vtable > (uint32_t)(len - 4)) return -1;
    vsize = rd_u16(buf + vtable);
    tsize = rd_u16(buf + vtable + 2);
    if (vsize < 4 || (vsize & 1u) || vsize > (uint32_t)(len - vtable) ||
        tsize < 4 || tsize > (uint32_t)(len - table)) return -1;
    *vtable_out = vtable; *vsize_out = vsize; *tsize_out = tsize;
    return 0;
}

/* Vtable slot for field idx, or 0 when the field is absent/default. */
static uint32_t field_at(const uint8_t *buf, uint32_t vtable,
                         uint32_t vsize, int idx)
{
    if (vsize < (uint32_t)(4 + 2 * idx + 2)) return 0;
    return rd_u16(buf + vtable + 4 + 2 * idx);
}

static flatcc_builder_ref_t build_GeoPoint(flatcc_builder_t *B, const struct GeoPoint *m)
{
    flatcc_builder_ref_t root;
    if (flatcc_builder_start_table(B, 2)) return 0;
    /* field 0: lat */
    {
        double *slot = (double *)flatcc_builder_table_add(B, 0, 8, 8);
        if (!slot) return 0;
        double tmp_v = (double)(m->lat);
        uint64_t tmp_u; memcpy(&tmp_u, &tmp_v, 8);
        flatbuffers_uint64_write_to_pe((uint64_t *)slot, tmp_u);
    }
    /* field 1: lon */
    {
        double *slot = (double *)flatcc_builder_table_add(B, 1, 8, 8);
        if (!slot) return 0;
        double tmp_v = (double)(m->lon);
        uint64_t tmp_u; memcpy(&tmp_u, &tmp_v, 8);
        flatbuffers_uint64_write_to_pe((uint64_t *)slot, tmp_u);
    }
    root = flatcc_builder_end_table(B);
    return root;
}

static flatcc_builder_ref_t build_Telemetry(flatcc_builder_t *B, const struct Telemetry *m)
{
    flatcc_builder_ref_t root;
    /* field 0: device (string) */
    flatcc_builder_ref_t device_ref =
        flatcc_builder_create_string_str(B, m->device ? m->device : "");
    if (!device_ref) return 0;
    /* field 2: pos (nested GeoPoint) */
    flatcc_builder_ref_t pos_ref = 0;
    if (m->pos) {
        pos_ref = build_GeoPoint(B, m->pos);
        if (!pos_ref) return 0;
    }
    /* field 3: samples ([float]) */
    flatcc_builder_ref_t samples_ref = 0;
    if (m->samples && m->samples_count > 0) {
        samples_ref = flatcc_builder_create_vector(
            B, m->samples, m->samples_count, 4, 4, 16777215);
        if (!samples_ref) return 0;
    }
    /* field 4: flags ([ubyte]) */
    flatcc_builder_ref_t flags_ref = 0;
    if (m->flags && m->flags_count > 0) {
        flags_ref = flatcc_builder_create_vector(
            B, m->flags, m->flags_count, 1, 1, 16777215);
        if (!flags_ref) return 0;
    }
    if (flatcc_builder_start_table(B, 5)) return 0;
    /* field 0: device */
    {
        flatcc_builder_ref_t *pref = flatcc_builder_table_add_offset(B, 0);
        if (!pref) return 0;
        *pref = device_ref;
    }
    /* field 1: fix (enum FixType) */
    {
        int8_t *slot = (int8_t *)flatcc_builder_table_add(B, 1, 1, 1);
        if (!slot) return 0;
        *slot = (int8_t)(m->fix);
    }
    /* field 2: pos */
    if (pos_ref) {
        flatcc_builder_ref_t *pref = flatcc_builder_table_add_offset(B, 2);
        if (!pref) return 0;
        *pref = pos_ref;
    }
    /* field 3: samples */
    if (samples_ref) {
        flatcc_builder_ref_t *pref = flatcc_builder_table_add_offset(B, 3);
        if (!pref) return 0;
        *pref = samples_ref;
    }
    /* field 4: flags */
    if (flags_ref) {
        flatcc_builder_ref_t *pref = flatcc_builder_table_add_offset(B, 4);
        if (!pref) return 0;
        *pref = flags_ref;
    }
    root = flatcc_builder_end_table(B);
    return root;
}

static FACE_TSS_RETURN_CODE parse_GeoPoint(const uint8_t *buf, size_t len, uint32_t table, struct GeoPoint *m)
{
    uint32_t vtable, vsize, tsize, entry, at;
    FACE_TSS_RETURN_CODE rc;
    if (table_header(buf, len, table, &vtable, &vsize, &tsize))
        return FACE_TSS_RC_INVALID_PARAM;
    /* field 0: lat */
    entry = field_at(buf, vtable, vsize, 0);
    if (entry) {
        if (entry + 8 > tsize)
            return FACE_TSS_RC_INVALID_PARAM;
        at = table + entry;
        if (at > (uint32_t)(len - 8))
            return FACE_TSS_RC_INVALID_PARAM;
        memcpy(&m->lat, buf + at, 8);
    }
    /* field 1: lon */
    entry = field_at(buf, vtable, vsize, 1);
    if (entry) {
        if (entry + 8 > tsize)
            return FACE_TSS_RC_INVALID_PARAM;
        at = table + entry;
        if (at > (uint32_t)(len - 8))
            return FACE_TSS_RC_INVALID_PARAM;
        memcpy(&m->lon, buf + at, 8);
    }
    (void)rc;
    return FACE_TSS_RC_NO_ERROR;
}

static FACE_TSS_RETURN_CODE parse_Telemetry(const uint8_t *buf, size_t len, uint32_t table, struct Telemetry *m)
{
    uint32_t vtable, vsize, tsize, entry, at;
    FACE_TSS_RETURN_CODE rc;
    if (table_header(buf, len, table, &vtable, &vsize, &tsize))
        return FACE_TSS_RC_INVALID_PARAM;
    /* field 0: device */
    entry = field_at(buf, vtable, vsize, 0);
    if (entry) {
        uint32_t off, n, start;
        if (entry + 4 > tsize)
            return FACE_TSS_RC_INVALID_PARAM;
        at = table + entry;
        if (at > (uint32_t)(len - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        off = rd_u32(buf + at);
        if (off == 0 || off > (uint32_t)(len - at - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        at += off;
        if (at > (uint32_t)(len - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        n = rd_u32(buf + at); start = at + 4;
        if (n + 1 > len - start || buf[start + n] != '\0')
            return FACE_TSS_RC_INVALID_PARAM;
        m->device = (char *)malloc(n + 1);
        if (!m->device) { Telemetry_fini(m);
            return FACE_TSS_RC_NOT_AVAILABLE; }
        memcpy(m->device, buf + start, n);
        m->device[n] = '\0';
    }
    /* field 1: fix */
    entry = field_at(buf, vtable, vsize, 1);
    if (entry) {
        if (entry + 1 > tsize)
            return FACE_TSS_RC_INVALID_PARAM;
        at = table + entry;
        if (at > (uint32_t)(len - 1))
            return FACE_TSS_RC_INVALID_PARAM;
        { int8_t tmp; memcpy(&tmp, buf + at, 1);
          m->fix = (FixType)tmp; }
    }
    /* field 2: pos */
    entry = field_at(buf, vtable, vsize, 2);
    if (entry) {
        uint32_t off, nat;
        if (entry + 4 > tsize)
            return FACE_TSS_RC_INVALID_PARAM;
        at = table + entry;
        if (at > (uint32_t)(len - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        off = rd_u32(buf + at);
        if (off == 0 || off > (uint32_t)(len - at - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        nat = at + off;
        m->pos = (struct GeoPoint *)calloc(1, sizeof(*m->pos));
        if (!m->pos) { Telemetry_fini(m);
            return FACE_TSS_RC_NOT_AVAILABLE; }
        rc = parse_GeoPoint(buf, len, nat, m->pos);
        if (rc != FACE_TSS_RC_NO_ERROR) { Telemetry_fini(m); return rc; }
    }
    /* field 3: samples */
    entry = field_at(buf, vtable, vsize, 3);
    if (entry) {
        uint32_t off, vat, n, start;
        if (entry + 4 > tsize)
            return FACE_TSS_RC_INVALID_PARAM;
        at = table + entry;
        if (at > (uint32_t)(len - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        off = rd_u32(buf + at);
        if (off == 0 || off > (uint32_t)(len - at - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        vat = at + off;
        if (vat > (uint32_t)(len - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        n = rd_u32(buf + vat); start = vat + 4;
        if (n > (len - start) / 4)
            return FACE_TSS_RC_INVALID_PARAM;
        if (n > 0) {
            m->samples = (float *)malloc((size_t)n * 4);
            if (!m->samples) { Telemetry_fini(m);
                return FACE_TSS_RC_NOT_AVAILABLE; }
            memcpy(m->samples, buf + start, (size_t)n * 4);
            m->samples_count = n;
        }
    }
    /* field 4: flags */
    entry = field_at(buf, vtable, vsize, 4);
    if (entry) {
        uint32_t off, vat, n, start;
        if (entry + 4 > tsize)
            return FACE_TSS_RC_INVALID_PARAM;
        at = table + entry;
        if (at > (uint32_t)(len - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        off = rd_u32(buf + at);
        if (off == 0 || off > (uint32_t)(len - at - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        vat = at + off;
        if (vat > (uint32_t)(len - 4))
            return FACE_TSS_RC_INVALID_PARAM;
        n = rd_u32(buf + vat); start = vat + 4;
        if (n > (len - start) / 1)
            return FACE_TSS_RC_INVALID_PARAM;
        if (n > 0) {
            m->flags = (uint8_t *)malloc((size_t)n * 1);
            if (!m->flags) { Telemetry_fini(m);
                return FACE_TSS_RC_NOT_AVAILABLE; }
            memcpy(m->flags, buf + start, (size_t)n * 1);
            m->flags_count = n;
        }
    }
    (void)rc;
    return FACE_TSS_RC_NO_ERROR;
}

static void fini_GeoPoint(struct GeoPoint *m)
{
    if (!m) return;
}

static void fini_Telemetry(struct Telemetry *m)
{
    if (!m) return;
    free(m->device); m->device = NULL;
    if (m->pos) { fini_GeoPoint(m->pos);
        free(m->pos); m->pos = NULL; }
    free(m->samples); m->samples = NULL;
    m->samples_count = 0;
    free(m->flags); m->flags = NULL;
    m->flags_count = 0;
}

void Telemetry_fini(void *msg)
{
    fini_Telemetry((struct Telemetry *)msg);
}

FACE_TSS_RETURN_CODE Telemetry_serialize(
    const void *msg, uint8_t **payload_out, size_t *payload_len_out)
{
    const struct Telemetry *m = (const struct Telemetry *)msg;
    flatcc_builder_t builder;
    flatcc_builder_t *B = &builder;
    flatcc_builder_ref_t root;
    void *buf;
    size_t size;
    if (!msg || !payload_out || !payload_len_out)
        return FACE_TSS_RC_INVALID_PARAM;
    flatcc_builder_init(B);
    root = build_Telemetry(B, m);
    if (!root || flatcc_builder_start_buffer(B, 0, 0, 0) ||
        !flatcc_builder_end_buffer(B, root)) {
        flatcc_builder_clear(B);
        return FACE_TSS_RC_NOT_AVAILABLE; }
    buf = flatcc_builder_finalize_buffer(B, &size);
    flatcc_builder_clear(B);
    if (!buf || size == 0) {
        if (buf) flatcc_builder_free(buf);
        return FACE_TSS_RC_NOT_AVAILABLE; }
    /* Copy to caller-owned malloc buffer: the finalize buffer must be
     * released with flatcc_builder_free, so keep ownership explicit. */
    {
        uint8_t *copy = (uint8_t *)malloc(size);
        if (!copy) { flatcc_builder_free(buf);
            return FACE_TSS_RC_NOT_AVAILABLE; }
        memcpy(copy, buf, size);
        flatcc_builder_free(buf);
        *payload_out = copy;
    }
    *payload_len_out = size;
    return FACE_TSS_RC_NO_ERROR;
}

FACE_TSS_RETURN_CODE Telemetry_deserialize(
    const uint8_t *payload, size_t payload_len, void *msg_out)
{
    struct Telemetry *m = (struct Telemetry *)msg_out;
    const uint8_t *buf = payload;
    size_t len = payload_len;
    uint32_t root, table, vtable, vsize, tsize;
    int32_t soff;
    uint8_t *copy = NULL;
    FACE_TSS_RETURN_CODE rc;
    if (!msg_out)
        return FACE_TSS_RC_INVALID_PARAM;
    memset(m, 0, sizeof(*m));
    if (!buf || len == 0)
        return FACE_TSS_RC_INVALID_PARAM;
    if (((uintptr_t)buf & 3u) != 0u) {
        copy = (uint8_t *)malloc(len);
        if (!copy) return FACE_TSS_RC_NOT_AVAILABLE;
        memcpy(copy, buf, len); buf = copy;
    }
    if (len < 8 || len > (size_t)0xFFFFFFF0UL)
        { free(copy); return FACE_TSS_RC_INVALID_PARAM; }
    root = rd_u32(buf);
    if (root < 4 || root > (uint32_t)(len - 4))
        { free(copy); return FACE_TSS_RC_INVALID_PARAM; }
    table = root;
    soff = (int32_t)rd_u32(buf + table);
    vtable = table - (uint32_t)soff;
    if (vtable > (uint32_t)(len - 4))
        { free(copy); return FACE_TSS_RC_INVALID_PARAM; }
    vsize = rd_u16(buf + vtable);
    tsize = rd_u16(buf + vtable + 2);
    if (vsize < 4 || (vsize & 1u) || vsize > (uint32_t)(len - vtable) ||
        tsize < 4 || tsize > (uint32_t)(len - table))
        { free(copy); return FACE_TSS_RC_INVALID_PARAM; }
    rc = parse_Telemetry(buf, len, table, m);
    free(copy);
    return rc;
}

const FACE_TSS_TYPE_SUPPORT Telemetry_type_support = {
    TELEMETRY_MESSAGE_GUID,
    "FaceTSS.Telemetry",
    Telemetry_serialize,
    Telemetry_deserialize,
    Telemetry_fini,
    sizeof(struct Telemetry),
    65536, /* max_size */
};
